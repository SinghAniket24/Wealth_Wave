
import 'package:flutter/material.dart';

class WatchlistPage extends StatelessWidget {
  const WatchlistPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Watchlist Page'),// stock you might want you buy future so when you select a stock you added in the watchlist
    );
  }
}
